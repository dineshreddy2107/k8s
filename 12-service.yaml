apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    purpose: service-demo
spec:
  selector:
    project: roboshop
    purpose: service-demo
    environment: dev
  ports:
    - protocol: TCP
      port: 8080 # service port
      targetPort: 80 # container port
---
apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    project: roboshop
    purpose: service-demo
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx

